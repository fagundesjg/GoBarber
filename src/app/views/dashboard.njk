{% extends "_layouts/default.njk" %}

{% block body %}
  <div class="content">
    <strong> Olá, {{user.name}}</strong>
    <p>Inicie um agendamente escolhendo um profissional abaixo:</p>
    <ul class="providers">
      {% for provider in providers %}
        <li class="provider">
          <div>
            <img src="/files/{{ provider.avatar }}"/>
            <strong>{{provider.name}}</provider>
          </div>
          <a href="/app/appointments/new/{{provider.id}}">+</a>
        </li>
      {% endfor %}
    </ul>
    {% if user.provider === true %}
      <a href="/app/provider" class="appointments">Agendamentos</a>
    {% endif %}
    <a href="/app/logout" class="logout">Sair</a>
  {% endblock %}
